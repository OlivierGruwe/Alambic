"""alambic_core.ai — clients et utilitaires IA (OCR, classification, extraction).

Porté des modules flowerscan_lib.ia, adapté à Alambic (config depuis le modèle
Config, pas de dépendance flowerscan_lib).
"""
