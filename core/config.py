"""Configuration Alambic — lecture du .env via pydantic-settings.

À implémenter : une classe Settings(BaseSettings) qui charge les variables
d'environnement (DATABASE_URL, broker Celery, endpoint + clés MinIO, etc.).
Importée par tous les autres modules de core/.
"""
